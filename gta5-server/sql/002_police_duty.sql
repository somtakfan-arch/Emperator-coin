-- Смена переживает выход и рестарт.
--
-- Изначально это было сделано наоборот: рестарт снимал всех со службы. На
-- практике это значит, что после каждого перезапуска мент едет на участок
-- нажимать G, и форму с рангом он при этом считает потерянными.
ALTER TABLE police_officers
    ADD COLUMN IF NOT EXISTS on_duty TINYINT(1) NOT NULL DEFAULT 0;
