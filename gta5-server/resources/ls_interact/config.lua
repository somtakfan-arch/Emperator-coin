Config = {}

-- Клавиша взаимодействия. 38 = E.
Config.Key = 38

-- Курсор меню. Списком, потому что принимаем и стрелки, и WASD: в подсказке
-- написано W/S, а 172/173 - это стрелки, и на W меню не отзывалось.
Config.Up = { 172, 32 }        -- стрелка вверх, W
Config.Down = { 173, 33 }      -- стрелка вниз, S
Config.Enter = { 176, 201 }    -- Enter
Config.Back = { 177, 202 }     -- Esc, Backspace

-- Сколько строк помещается на экране за раз.
Config.Rows = 8

-- Подсказка "нажми E" висит, пока рядом есть хоть одно действие.
Config.ShowHint = true
